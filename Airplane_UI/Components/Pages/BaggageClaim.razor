@page "/baggagcelaim"
@using Airplane_UI.Contracts.LuggageMaintnance
@using MudBlazor
@rendermode InteractiveServer
@using System.Net.Http.Json
@inject IBaggageClaimService BaggageClaimService
@inject NavigationManager Navigation

<PageTitle>BaggageClaim</PageTitle>

<MudTable Items="@baggageClaims" Hover="true" Breakpoint="Breakpoint.Sm" Loading="@_loading" LoadingProgressColor="Color.Info">
    <ToolBarContent>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="@(() => Navigation.NavigateTo("/baggagcelaim/create"))" Class="mb-3">
            Create New Airline
        </MudButton>
    </ToolBarContent>
    <HeaderContent>
        <MudTh>Id</MudTh>
        <MudTh>CarouselNumber</MudTh>
        <MudTh>Status</MudTh>
        <MudTh>TerminalName</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="Id">@context.Id</MudTd>
        <MudTd DataLabel="Name">@context.CarouselNumber</MudTd>
        <MudTd DataLabel="IATA Code">@context.Status</MudTd>
        <MudTd DataLabel="IATA Code">@context.TerminalName</MudTd>
        <MudTd DataLabel="Actions">
            <MudButton Variant="Variant.Text" Color="Color.Primary" OnClick="@(() => NavigateToDetails(context.Id))">
                View Details
            </MudButton>
        </MudTd>
    </RowTemplate>
</MudTable>

@if (!string.IsNullOrEmpty(_errorMessage))
{
    <MudAlert Severity="Severity.Error" Class="mt-4">
        @_errorMessage
    </MudAlert>
}

